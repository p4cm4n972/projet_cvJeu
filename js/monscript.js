/*$(document).ready(function () {
var i = 0;
setInterval(function () {
$('.progress-bar').attr('style','width:' + i + '%');
i = i +1;},200);
});*/

/*$(document).ready(function () {
    $('.progress-bar').each(function () {
        $(this).animate({
            width : $(this).attr("aria-valuenow")+"%"
        },1000, function () {

        })
    })
})*/

/*$(document).ready(function () {

    $('.progress-bar').bind('inview', function (event, isInView, visiblePartX, visiblePartY) {
        if (isInView) {
            var i = 0;
            setInterval(function () {
                $('.progress-bar').attr('style', 'width:' + i + '%');
                i = i + 1;
            }, 200);
        }
    })
    })*/

/* $('.progress-bar').each(function (index) {
                $(this).animate({
                    width: $(this).delay(1000 * index).attr("aria-valuenow") + "%"
                }, 1000)*/

